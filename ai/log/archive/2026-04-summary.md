# 2026-04 Archive Summary

- entries: 63
- range: 2026-04-01T08:53:27.0301641+02:00 to 2026-04-09T14:35:00+02:00
- event types: feature=51, planning=4, process=3, product=2, benchmark=1, documentation=1, maintenance=1

## Event Snapshots
- `2026-04-01T08:53:27.0301641+02:00`: Moved the local Claude orchestration runtime to repo-local `.claude-orchestrator/`, updated copy-mode workspace creation to exclude that runtime tree for safe concurrent runs, added `ai/orchestrator/SYSTEM-SPEC.md` plus `ai/orchestrator/tasks/example-parallel.json` as the portable bootstrap and concurrency examples, and revalidated orchestrator dry-runs, docs consistency, and AI memory refresh/check.
- `2026-04-01T14:46:29.3747538+02:00`: Profiled the remaining SQL-like scatter gap with warmed GC and stack runs under `target/benchmarks/2026-04-01-sqllike-profile/`. Direct and bound SQL-like scatter stayed near parity (`1.007x`, `1.010x`, `1.096x` latency direct-vs-bound across `1k`/`10k`/`100k`), while SQL-like remained `1.470x` / `1.340x` / `1.872x` slower and `1.426x` / `1.566x` / `3.406x` more allocative than fluent. The focused stack sample on `sqlLikeScatterMapping|size=100000` concentrated in `ChartMapper.readField`, `ReflectionUtil.getFieldValue`, `ReflectionUtil.resolveFieldPath`, and `ChartMapper.MultiSeriesAccumulator`, indicating the remaining cost is reflection-heavy source-row chart mapping rather than bind/setup reuse.
- `2026-04-01T15:45:00+02:00`: Tightened `ai/orchestrator/tasks/sql-like-scatter-followup.json` into the tracked multi-agent plan for the SQL-like scatter follow-up. The plan now carries the concrete 2026-04-01 scatter deltas, includes the typed-bind parity test in the runtime task scope, validates as a five-task / three-batch DAG, and dry-runs cleanly with `--max-parallel 2` under repo-local `.claude-orchestrator/`.
- `2026-04-01T18:30:00+02:00`: Updated the Claude orchestrator for lower-cost runs: worker prompts now default to minimal task-local context, copy-mode workspace hydration only pulls explicit small file hints, agents and tasks support `modelProfile` (`simple`/`balanced`/`complex`), and dry-run or live run records now expose resolved model choice, prompt-size estimates, and aggregated Claude usage totals when the CLI returns usage fields.
- `2026-04-01T19:15:00+02:00`: Added root `SPIKE.md` exploring a controlled plain-English query surface beside fluent and SQL-like. The spike recommends a new explicit parser entry point, likely `PojoLensNatural`, with runtime-scoped field vocabulary and deterministic lowering into the existing engine rather than a new execution core or unconstrained natural-language behavior.
- `2026-04-01T19:35:00+02:00`: Added root `SPIKE-CSV.md` exploring CSV support. The spike concludes that CSV fits the repo only as a bounded adapter into the same in-memory engine, ideally typed-first, and becomes out-of-scope if it turns `pojo-lens` into a dataframe or general file-ingestion platform.
- `2026-04-02T10:29:36.3306897+02:00`: Implemented the first controlled plain-English query MVP. Added `PojoLensNatural`, `NaturalQuery`, `NaturalBoundQuery`, and `PojoLensRuntime.natural()`; parsed deterministic `show`/`where`/`sort by`/`limit`/`offset` queries with aliases and named params into shared `QueryAst`; reused the existing execution pipeline with natural-specific explain and telemetry; added parser/runtime/public-API coverage; and aligned README plus entry-point/product-surface/public-stability docs.
- `2026-04-02T12:45:00+02:00`: Completed the next plain-English query slice by adding runtime-scoped `NaturalVocabulary` binding on `PojoLensRuntime`. Natural queries now resolve field aliases against the actual source shape at execution/explain time, fail deterministically on ambiguity or unknown terms, expose `resolvedNaturalFields` plus `resolvedEquivalentSqlLike` in explain payloads, and keep the shared SQL-like execution path. Revalidated with `mvn -q -pl pojo-lens test` and `scripts/check-doc-consistency.ps1`.
- `2026-04-02T13:35:00+02:00`: Extended the plain-English surface with grouped aggregates. Natural queries now parse metric phrases (`count of`, `sum of`, `average of`, `min of`, `max of`), `group by`, and `having`, lower them into the shared aggregate SQL-like path, resolve metric arguments through runtime vocabulary, and expose grouped execution through parser/runtime/public-API/telemetry coverage. Revalidated with `mvn -q -pl pojo-lens test` and `scripts/check-doc-consistency.ps1`.
- `2026-04-02T16:10:00+02:00`: Completed the next plain-English slices by adding natural time-bucket phrases (`bucket <field> by ...`) and terminal chart phrases (`as <type> chart`). Natural queries now infer deterministic no-spec chart execution from `show` outputs, explain chart metadata alongside resolved natural fields, and ship with a new canonical guide at `docs/natural.md` plus parser/runtime/public-API/docs/telemetry coverage. Revalidated with `mvn -q -pl pojo-lens test` and `scripts/check-doc-consistency.ps1`.
- `2026-04-02T18:20:00+02:00`: Completed the next natural-query slice by adding explicit `from ... join ... on ...` phrasing with optional source labels, join-aware runtime vocabulary resolution, and shared `JoinBindings` / `DatasetBundle` execution for `PojoLensNatural`. The canonical natural guide plus README/entry-point/use-case docs now document the join contract, with parser/runtime/public-API/docs/telemetry coverage revalidated by `mvn -q -pl pojo-lens test` and `scripts/check-doc-consistency.ps1`.
- `2026-04-02T20:45:00+02:00`: Completed the next natural-query slice by adding deterministic window phrases (`row number`, `rank`, `dense rank`, running aggregates) plus alias-based `qualify`. Natural windows now resolve runtime vocabulary through partition/order/value fields, execution/public-API/telemetry coverage was added, and README/natural/entry-point/use-case docs were aligned and revalidated with `mvn -q -pl pojo-lens test` and `scripts/check-doc-consistency.ps1`.
- `2026-04-03T10:15:03.2489946+02:00`: Rebalanced `README.md` quick start to show one short example each for fluent, SQL-like, and natural queries, and moved grouped/joined/helper walkthrough emphasis back into the dedicated docs pages. Revalidated with `scripts/check-doc-consistency.ps1`.
- `2026-04-03T10:15:03.2489946+02:00`: Extended the natural-query Phase 4 slice with reusable `NaturalTemplate` parameter schemas plus plain-worded runtime-scoped computed-field coverage. Added `PojoLensNatural.template(...)` and `NaturalRuntime.template(...)`, documented deterministic natural template binding and computed-field wording in `docs/natural.md`, aligned entry-point/product-surface/public-stability docs, and revalidated with `mvn -q -pl pojo-lens test` plus `scripts/check-doc-consistency.ps1`.
- `2026-04-03T10:15:03.2489946+02:00`: Added root `SPIKE-LIMITATIONS.md` to turn the current documented engine limits into a scoped remediation spike. The spike recommends broadening time-bucket input types first, then widening uncorrelated single-column grouped/aggregate SQL-like subqueries, while treating aggregate ORDER BY mostly as a docs/diagnostics issue and builder mutability as a wrapper-design issue rather than a thread-safe-builder rewrite.
- `2026-04-03T13:56:00+02:00`: Added root `SPIKE-AI-MULTI-AGENT.md` to scope the remaining local orchestrator work around live-run proof, sparse copy mode, overlap/path safety, coordinator review or retry or cleanup helpers, and Python regression coverage. Revalidated the current contract with `scripts/claude-orchestrator.ps1 validate ... --json`, `scripts/claude-orchestrator.ps1 run ... --dry-run --max-parallel 2 --json`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-03T14:24:00+02:00`: Completed the planned `SPIKE.md` scope by adding `ReportDefinition.natural(...)` so natural queries can promote into reusable row/chart wrappers. Added reusable-wrapper/public-API/docs coverage, aligned `docs/natural.md`, `docs/reports.md`, `docs/entry-points.md`, `docs/reusable-wrappers.md`, `docs/modules.md`, and marked Phase 4 done after `mvn -q -pl pojo-lens test` plus `scripts/check-doc-consistency.ps1` passed.
- `2026-04-03T14:47:00+02:00`: Added bounded optional filler-word tolerance to the natural parser: `show me` and leading `the`/`a`/`an` before source or field references now lower to the same deterministic query shape without becoming mandatory syntax. Added parser/runtime coverage, updated `SPIKE.md` and `docs/natural.md`, and revalidated with `mvn -q -pl pojo-lens test` plus `scripts/check-doc-consistency.ps1`.
- `2026-04-03T14:58:00+02:00`: Extended the bounded natural-grammar sugar with operator-inflection aliases: `containing`, `starting with`, and `ending with` now map to the existing `contains` / `starts with` / `ends with` behavior. Added parser/runtime coverage, updated `SPIKE.md` and `docs/natural.md`, and revalidated with `mvn -q -pl pojo-lens test` plus `scripts/check-doc-consistency.ps1`.
- `2026-04-03T18:36:51.2426944+02:00`: Extended the bounded natural grammar with connector/clause/comparison aliases: `who|that|which is|are` now introduce deterministic filter clauses with bounded boolean shorthand, top-level `ordered by`, `grouped by`, `as a|an <chart> chart`, and explicit comparison phrases like `equal to` / `greater than or equal to` now lower to the existing engine. Updated `SPIKE.md`, refreshed `docs/natural.md` examples, added parser/runtime/docs coverage, and revalidated with `mvn -q -pl pojo-lens test` plus `scripts/check-doc-consistency.ps1`.
- `2026-04-03T20:09:16.0841939+02:00`: Removed the completed root `SPIKE.md` now that the natural-query work is represented by shipped code plus the canonical `docs/natural.md` guide. Updated hot AI state to stop treating `SPIKE.md` as a live artifact and revalidated with `scripts/check-doc-consistency.ps1`.
- `2026-04-03T20:31:06.3950945+02:00`: Refined `SPIKE-AI-MULTI-AGENT.md` using lessons from `drona23/claude-token-efficient`: keep recurring planner/worker prompt scaffolding tiny, treat JSON-only minimal output as a hard contract, add prompt-budget and prompt-compaction work as an explicit phase, and avoid assuming a large repo-wide `CLAUDE.md` overlay will help fresh per-worker sessions without measurement.
- `2026-04-03T21:14:00.8068659+02:00`: Completed the first live local-orchestrator proof slice. A real `scripts/claude-orchestrator.ps1 run ai/orchestrator/tasks/example-review.json --json` run initially exposed a CLI integration bug where variadic tool flags consumed the prompt argument; fixing `claude_command(...)` to compact tool lists and terminate options with `--` resolved it. Added `scripts/tests/test_claude_orchestrator.py` to lock the command shape, reran the live `copy`-mode example successfully, and updated `SPIKE-AI-MULTI-AGENT.md` so prompt economy is now the next priority.
- `2026-04-04T09:05:00+02:00`: Advanced the local orchestrator prompt-economy slice: planner and worker prompts now emit section-level prompt metrics, optional `maxPromptEstimatedTokens` / `maxPromptChars` fail oversized prompts before Claude runs, dependency and list sections are compacted, and `ai/orchestrator/agents.json` now carries leaner JSON-only role prompts with default token ceilings. Revalidated with Python compile/tests, orchestrator validate plus dry-run commands for `example-review.json` and `example-parallel.json`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T10:50:00+02:00`: Advanced the local orchestrator safety slice: validate/run now detect `parallelConflicts`, dependency-ready write-capable tasks with overlapping declared scopes are serialized conservatively, task records capture `actual_files_touched` from workspace diffs, and protected-path edits (`TODO.md`, `ai/state/*`, `ai/log/*`, `ai/indexes/*`) now fail the task record. Revalidated with Python compile/tests, orchestrator validate plus dry-run commands for `example-review.json` and `example-parallel.json`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T12:55:00+02:00`: Advanced the copy-mode workspace slice: `prepare_workspace(...)` now builds true sparse-copy workspaces seeded from `AGENTS.md`, `ai/AGENTS.md`, and explicit file hints only instead of cloning nearly the whole repo first. Added regression coverage for sparse hydration, updated the orchestrator docs/spike contract, and revalidated with Python compile/tests, orchestrator validate plus dry-run commands for `example-review.json` and `example-parallel.json`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T13:30:00+02:00`: Advanced the coordinator review slice: `scripts/claude-orchestrator.py` now supports `review` for per-task workspace diff summaries and `export-patch` for unified diff export from copy/worktree runs. Added regression coverage for run-manifest review and patch export, updated the orchestrator docs/spike contract, and revalidated with Python compile/tests, `validate`, dry-run `run`, `review`, `export-patch`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T15:03:00+02:00`: Restored the coordinator adoption slice after a bad commit dropped it: `scripts/claude-orchestrator.py` again supports conservative `promote` for copy/worktree run manifests, refusing protected-path violations, repo-mode records, path traversal, and duplicate changed-file ownership before copying reviewed workspace changes back into the repo. Regression coverage for live promotion plus duplicate-file rejection was restored, the orchestrator docs/spike contract were resynced, and validation was rerun with Python compile/tests, `validate`, dry-run `run`, `review`, `export-patch`, `promote --dry-run`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T16:58:00+02:00`: Advanced the coordinator lifecycle slice: `scripts/claude-orchestrator.py` now supports manifest-driven `retry`, which seeds completed dependency records from a prior run while rerunning unfinished dependencies, and run-scoped `cleanup`, which removes runtime artifacts plus detached worktrees for a run. Added regression coverage for dependency seeding, dependency requeueing, and cleanup, updated the orchestrator docs/spike contract, and revalidated with Python compile/tests, `validate`, dry-run `run`, `review`, `export-patch`, `promote --dry-run`, `retry --dry-run`, `cleanup`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T18:36:00+02:00`: Advanced the coordinator final-validation slice: `scripts/claude-orchestrator.py` now supports `validate-run`, which dedupes worker-suggested `validationCommands`, can execute them from repo root, writes stdout/stderr artifacts under the run `validation/` directory, and persists coordinator validation results separately back into the run manifest. Added regression coverage for dry-run aggregation plus live success/failure execution behavior, updated the orchestrator docs/spike contract, and revalidated with Python compile/tests, `validate`, dry-run `run`, `review`, `export-patch`, `promote --dry-run`, `retry --dry-run`, `validate-run --dry-run`, `cleanup`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T19:40:01.1834247+02:00`: Expanded orchestrator failure-path regression coverage in `scripts/tests/test_claude_orchestrator.py`: added explicit tests for malformed worker stdout that lacks standalone JSON, fail-fast scheduling that blocks remaining ready tasks after a worker failure, and dependency blocking that still allows independent tasks to continue when `--continue-on-error` is enabled. Revalidated with Python compile/tests and `scripts/check-doc-consistency.ps1`, and advanced hot memory so live prompt-budget tuning is now the next AI orchestration slice.
- `2026-04-04T20:54:00.0385623+02:00`: Completed the live prompt-budget tuning slice for the local orchestrator. A real `copy`-mode run first exposed a missing `file_sha256(...)` helper in workspace auditing; after restoring it and adding regression coverage, the tracked `example-review.json` and `example-parallel.json` live runs completed successfully. The tuning result was not lower prompt ceilings: live prompts stayed within budget, but workers needed bounded dependency handoff plus stricter caps on `summary`/`notes`/`followUps`/`validationCommands` to avoid blocked reviews and verbose structured output. Revalidated with Python compile/tests, live `run --json` on both tracked plans, plan validation, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T21:42:54.0865415+02:00`: Advanced the orchestrator validation-policy slice: `validate-run` now defaults to completed-task commands only unless `--include-status` explicitly broadens the policy, and worker JSON is normalized coordinator-side so invalid statuses fail fast while oversized `summary`/`notes`/`followUps`/`validationCommands` are compacted before task records are written. Added regression coverage for verbose worker output compaction, invalid status rejection, and completed-only validation filtering, then revalidated with Python compile/tests, a live `example-review.json` run, `validate-run --dry-run`, and `scripts/check-doc-consistency.ps1`.
- `2026-04-04T22:18:55.1289306+02:00`: Advanced validation-command quality controls for the local orchestrator. `validate-run` now classifies each worker-suggested command, rejecting shell composition and unknown entrypoints by default unless `--allow-unsafe-commands` is used explicitly. Worker prompts and agent definitions now steer validation suggestions toward direct repo-script or approved tool invocations. Added regression coverage for command policy acceptance/rejection and completed-only filtering, then revalidated with Python compile/tests, tracked plan validation, a live `example-review.json` run, and `validate-run --dry-run`, which now reports the live worker's piped `grep` suggestions as rejected.
- `2026-04-05T08:59:44.9552232+02:00`: Completed the next worker-result contract slice for the local orchestrator. `scripts/claude-orchestrator.py` now treats `filesTouched`, `validationCommands`, `followUps`, and `notes` as nullable list fields where `[]` means known-empty and `null` means genuinely unknown; task records persist that distinction in `unknown_fields`, dependency handoff and validation review surfaces now expose the unknown state explicitly, and workspace auditing no longer treats worker-unknown `filesTouched` the same as omitted reports. Added regression coverage for null-vs-empty coercion, unknown dependency handoff, and unknown validation-command aggregation, updated the orchestrator docs/spike/hot memory contract, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T10:01:27+02:00`: Advanced the orchestrator validation surface with structured `validationIntents`. Workers may now emit `repo-script` and `tool` intents beside legacy raw `validationCommands`; `validate-run` dedupes both forms together, preserves structured suggestions in task/manifest review payloads, and executes accepted intents with shell-free argv handling while keeping the existing policy gates for raw commands. Added regression coverage for intent normalization, policy acceptance/rejection, duplicate intent-vs-command aggregation, and live intent execution, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T10:48:34+02:00`: Collapsed safe raw validation commands onto the same structured execution path as `validationIntents`. `scripts/claude-orchestrator.py` now parses accepted direct tool or repo-script `validationCommands` into normalized intents, preserves the original command text for review output, and executes those accepted legacy commands without `shell=True`; only unsafe or explicitly overridden raw commands stay on the shell path. Added regression coverage for normalized raw-command policy payloads, raw-command aggregation metadata, and argv-backed execution reporting, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T12:30:36.2244368+02:00`: Added live slop-status progress reporting to the local Claude orchestrator. Planner, worker, copy/worktree prep, and coordinator validation execution now use phase-tagged interactive-`stderr` heartbeats such as `Slopchurning`, `Slopsloshing`, `Slurping up slop`, and `Slopcrunching` while subprocesses are in flight, with `stdout` left machine-readable for JSON consumers. Added regression coverage for the poll-based progress runner and command-summary compaction, updated the tracked orchestration docs/hot state, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T13:34:35.2519332+02:00`: Made raw worker `validationCommands` an explicit compatibility path in the local orchestrator. `validate-run` now supports `--intents-only` to reject legacy raw commands even when they normalize cleanly, validation task/summary payloads now expose legacy-command counts and task ids, result records separate compatibility-only rejection from the underlying quality policy, and worker prompts plus agent definitions now describe raw commands as a deprecated fallback behind structured `validationIntents`. Added regression coverage for legacy-command accounting and the new `--intents-only` gate, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run, a dry-run `validate-run --intents-only`, docs consistency, and AI memory refresh/check.
- `2026-04-05T14:25:42.2334538+02:00`: Extended intent-only validation enforcement into live worker execution. `run` and `retry` now accept `--worker-validation-mode`, worker prompts can switch to an intent-only contract, task execution rejects non-empty raw `validationCommands` when mode is `intents-only`, retry inherits the source manifest mode unless overridden, and run manifests plus summaries now record `workerValidationMode`. Added regression coverage for intent-only worker prompts, raw-command rejection during task execution, and retry-mode inheritance, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run under `--worker-validation-mode intents-only`, docs consistency, and AI memory refresh/check.
- `2026-04-05T15:42:14.3297412+02:00`: Moved live worker-validation policy from a runtime-only flag into tracked orchestration definitions. `scripts/claude-orchestrator.py` now accepts task and agent `workerValidationMode`, treats `--worker-validation-mode` as an explicit override, records summarized plus per-task mode data in run manifests, and lets retry preserve explicit source-run overrides while older manifests still fall back cleanly. Updated the tracked example task plans to declare `workerValidationMode = intents-only`, refreshed the orchestrator docs and hot-state wording, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T17:13:29.1180016+02:00`: Codified agent-level worker-validation defaults for the tracked read-only orchestrator roles. `ai/orchestrator/agents.json` now sets `workerValidationMode = intents-only` for `analyst` and `reviewer`, the tracked sample task plans inherit those defaults instead of repeating task-level settings, `scripts/claude-orchestrator.py` now reports effective per-task worker-validation sources in `validate` output plus run/manifests, and task records now persist `worker_validation_mode_source`. Added regression coverage for validate-summary source reporting and run-payload source metadata, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T17:41:37.0093292+02:00`: Extended intent-only worker-validation defaults to the tracked `implementer` role. `ai/orchestrator/agents.json` now defaults every tracked worker role except `planner` to `workerValidationMode = intents-only`, while task-level `workerValidationMode = compat` remains the explicit escape hatch for rare validation suggestions that cannot be expressed as structured intents. Updated the regression fixture that models task-level overrides, refreshed the orchestrator docs/spike state, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T18:15:25.6592378+02:00`: Tightened the live worker contract for intent-only runs by making the Claude JSON schema mode-aware. `scripts/claude-orchestrator.py` now emits an `intents-only` worker schema that limits `validationCommands` to `[]` or `null`, so raw legacy command items are blocked at the schema boundary before the coordinator's parse-time rejection runs. Added regression coverage for the mode-specific schema JSON and refreshed the orchestrator docs/spike state, then revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T19:42:21.4253068+02:00`: Made remaining compat-mode worker-validation usage explicit in local orchestrator summaries. `scripts/claude-orchestrator.py` now reports `compatWorkerValidationTaskIds` and `compatWorkerValidationTaskCount` in `validate --json`, run payloads, and manifests, while planner guidance now treats `workerValidationMode = compat` as a deprecated exception for legacy validation suggestions that still cannot be expressed as structured intents. Added regression coverage for compat-task accounting in validate and run payloads, resynced the orchestrator docs/spec/spike state, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T20:41:43.5219924+02:00`: Added a strict zero-compat gate for tracked orchestrator plans. `scripts/claude-orchestrator.py` now accepts `--require-intents-only-workers` on `validate`, `run`, and `retry`, failing fast when any effective task still resolves to `workerValidationMode = compat` while leaving the compatibility path available by default for exceptional legacy plans. Added regression coverage for strict validate rejection, strict run rejection, and retry forwarding, updated the orchestrator docs/spec/spike state, and revalidated with Python compile/tests, strict tracked-plan validate plus dry-run run, docs consistency, and AI memory refresh/check.
- `2026-04-05T21:51:52.5969140+02:00`: Rewrote `SPIKE-AI-MULTI-AGENT.md` from broad historical phases into concrete open work packages. The spike is now framed around `WP4` removing raw worker `validationCommands` from the live contract, `WP6` running a non-trivial live workflow proof, `WP5` widening validation-intent kinds only if that live proof exposes a real gap, then `WP7` operational cleanup and `WP8` regression expansion. Hot-state files were compacted to keep the new planning context under the enforced memory budgets.
- `2026-04-06T09:47:49.1913608+02:00`: Completed `WP4` for the local orchestrator spike. `scripts/claude-orchestrator.py` now makes live worker validation structured-intent-only: worker prompts/schemas require `validationIntents`, non-empty raw `validationCommands` are rejected universally during live worker parsing, live `workerValidationMode = compat` is no longer accepted for agent/task/CLI config, and retry no longer replays old manifest-level compat mode into live workers. Legacy raw commands remain available only for older manifests and review-time `validate-run` handling. Updated orchestrator docs/spec/spike state, refreshed hot memory, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, doc consistency, and AI memory refresh/check.
- `2026-04-06T13:56:39.3610739+02:00`: Completed `WP6` for the local orchestrator spike with a real implementer workflow. Added tracked plan `ai/orchestrator/tasks/wp6-live-parser-proof.json`, ran it live through `review`, `export-patch`, `validate-run`, `promote --dry-run`, and final `promote`, and adopted a bounded two-file patch that removes the last live compat exception in `coerce_task_run_record()` plus adds parser-level regressions in `scripts/tests/test_claude_orchestrator.py`. The run showed that `tool` intents were sufficient for the real validation suggestion, reviewer-emitted `repo-script` misuse was correctly rejected by policy, reviewer accuracy on changed-file inspection is still a risk, and repo-root `validate-run` sequencing remains an operational limitation. Updated the spike/hot-state docs and revalidated with Python compile/tests, tracked-plan validate, doc consistency, and AI memory refresh/check.
- `2026-04-07T10:53:41+02:00`: Completed `WP9` for the local orchestrator spike. Task plans now use explicit `sharedContext.readPaths`, task `readPaths`, and task `writePaths`; copy-mode validation fails missing or directory read inputs and surfaces oversized hydration files; overlapping write-capable tasks serialize by declared `writePaths`; workspace audit records `write_scope_violations`; and promotion/docs/sample plans now reflect the stronger scope contract. Revalidated with Python compile/tests, tracked-plan validate for `example-review.json`, `example-parallel.json`, and `wp6-live-parser-proof.json`, plus `example-parallel.json --dry-run` and docs consistency.
- `2026-04-07T13:17:57.9995452+02:00`: Completed `WP10` for the local orchestrator spike. `scripts/claude-orchestrator.py` now supports opt-in `dependencyMaterialization = "apply-reviewed"` for downstream `copy`/`worktree` tasks, replays reviewed dependency layers into downstream workspaces before worker execution, rejects ambiguous direct-dependency overlaps, and records `dependency_materialization_mode` plus `dependency_layers_applied` in task records, review output, promotion summaries, and retry manifests. Added regression coverage for materialization validation and workspace layering, introduced tracked sample plan `ai/orchestrator/tasks/example-materialized-chain.json`, updated the orchestrator guide/spec and spike/hot-state docs, and revalidated with Python compile/tests, tracked-plan validate plus dry-run run, doc consistency, and AI memory refresh/check.
- `2026-04-07T14:41:52.6483503+02:00`: Completed `WP13` for the local orchestrator spike with retained live run `20260407T120023Z-wp13-live-materialized-prompt-proof-87dda84c`. Added tracked live plan `ai/orchestrator/tasks/wp13-live-materialized-prompt-proof.json`, ran it through live `run`, `review`, `export-patch`, `validate-run`, and downstream-only `promote`, and adopted a bounded same-file regression slice in `scripts/tests/test_claude_orchestrator.py` that makes dependency-materialization prompt and validate/manifest surfaces visible in tests. The run proved that `apply-reviewed` layering records the upstream workspace layer and that promoting only the downstream workspace is enough to adopt the cumulative change, while also surfacing two operational findings: task-workspace validation still depends on declared sparse-copy inputs for runtime-loaded files, and reviewer false positives remain a real risk. Updated orchestrator docs plus spike/hot-state memory and revalidated with Python compile/tests, doc consistency, and AI memory refresh/check.
- `2026-04-07T15:19:40+02:00`: Reduced per-task worker prompt overhead in the local orchestrator. `scripts/claude-orchestrator.py` now keeps shared `readPaths` out of minimal-mode prompt bodies and renders stable coordinator sections before run-specific workspace context so provider-side prefix caching can reuse more of each request. Added regressions proving minimal vs full read-context prompt behavior, updated the orchestrator guide/spec, and revalidated with Python compile/tests plus a tracked WP13 dry-run whose total prompt estimate dropped from `3790` to `3742` tokens.
- `2026-04-07T16:59:27.1087119+02:00`: Reduced worker-request overhead in the local orchestrator again. `scripts/claude-orchestrator.py` now sends only the selected agent definition to live Claude invocations, uses stable workspace labels instead of absolute filesystem paths in worker execution-context text, and keeps the repeated worker-rules block compact enough to avoid prompt truncation. Added regressions for selected-agent payloads, cache-friendly execution context, and untruncated worker rules, updated the orchestrator guide/spec plus hot state, and revalidated with Python compile/tests, the tracked WP13 dry-run, doc consistency, and AI memory refresh/check; the tracked dry-run prompt estimate fell from `3742` to `3193` total tokens (`3790` before the overhead work started).
- `2026-04-07T17:53:02+02:00`: Tightened expensive-model policy in the local orchestrator. Planner guidance now treats `modelProfile = "complex"` / `claude-opus-4-6` as the exceptional path rather than a normal choice, and `validate --json`, `run --json`, plus run manifests now expose resolved `taskModels`, `taskModelProfiles`, and `complexModelTaskIds` so accidental `opus` usage is obvious before or during a run. Added regression coverage for validate and dry-run manifest visibility, confirmed tracked plans still resolve entirely to `claude-haiku-4-5` and `claude-sonnet-4-6`, and revalidated with Python compile/tests, tracked validate plus dry-run run, doc consistency, and AI memory refresh/check.
- `2026-04-07T19:57:27.9966628+02:00`: Completed `WP11` for the local orchestrator spike. `scripts/claude-orchestrator.py` now supports same-run `resume` from a retained run manifest, runtime-root `inventory`, and age-based `prune`; resume prefers the run-local `selected-plan.json` snapshot, preserves completed records, and reuses the same run id and runtime directories, while prune skips incomplete runs by default unless the operator opts in. Added regression coverage for in-place run reuse, resume preservation, retained-run inventory ordering, prune age filtering, and nullable manifest worker-validation overrides, then revalidated with Python compile/tests, tracked validate, retained-run resume dry-run, inventory/prune dry-runs, doc consistency, and AI memory refresh/check.
- `2026-04-08T16:08:00+02:00`: Reevaluated the local orchestrator around prompt footprint and default pipeline size. `scripts/claude-orchestrator.py` now keeps per-task worker prompts focused on coordinator/workspace rules while leaving role-stable output discipline in the selected agent definition, and planner guidance plus `ai/orchestrator/agents.json` now prefer the smallest viable actor set instead of defaulting to analyst/reviewer hops. Updated the tracked orchestration docs, spike, and hot-state memory; Python compile/tests and `scripts/check-doc-consistency.ps1` passed; and the tracked `wp13-live-materialized-prompt-proof` dry-run prompt estimate fell from `3193` to `2697` total tokens.
- `2026-04-08T17:00:00+02:00`: Completed `WP12` for the local orchestrator spike by separating coordinator memory from worker runtime contract. `scripts/claude-orchestrator.py` no longer auto-seeds `AGENTS.md` or `ai/AGENTS.md` into `copy` workspaces, worker prompts now treat the prompt plus declared workspace as the full contract, and tracked sample plans now pass coordinator-memory files only when a task explicitly needs them. Updated the spike, orchestrator guide/spec, and hot-state memory to move the next active package to `WP14` run-level budget/artifact governance. Revalidated with Python compile/tests, tracked plan validation for `example-review.json`, `example-parallel.json`, `example-materialized-chain.json`, and `wp6-live-parser-proof.json`, plus the tracked `wp13-live-materialized-prompt-proof` dry-run; the current dry-run prompt estimate is `2745` total tokens.
- `2026-04-08T18:30:00+02:00`: Completed `WP14` for the local orchestrator spike. `scripts/claude-orchestrator.py` now supports top-level task-plan `runPolicy` for aggregate run budget and per-task stdout/stderr/result artifact limits, with `warn` or between-batch `stop` behavior; `validate --json` surfaces tracked `runPolicy`, while run payloads and manifests now expose `runGovernance` summaries with alert counts, blocking alerts, highest-cost tasks, and aggregate artifact totals. Added regression coverage for budget-stop, budget-warn, and artifact-stop scheduling behavior, updated the orchestrator guide/spec plus spike/hot-state memory, and revalidated with Python compile/tests, doc consistency, and AI memory refresh/check.
- `2026-04-08T19:05:00+02:00`: Completed `WP15` for the local orchestrator spike. `scripts/claude-orchestrator.py` now emits compact plan `topology` summaries in `validate --json`, run payloads, and manifests, plus topology batch/parallel-warning fields in retained-run summaries. The coordinator now reports agent mix, read-only vs write-capable task counts, batch sizes, max dependency hops, and conservative lean-plan warnings for obviously overweight DAGs. Added regression coverage for direct topology warnings, validate-surface visibility, and run/manifest/summary propagation, then revalidated with Python compile/tests, doc consistency, and AI memory refresh/check.
- `2026-04-08T19:40:00+02:00`: Deflated the tracked lean-plan samples after `WP15`. `ai/orchestrator/tasks/example-review.json` is now a one-task reviewer sample with no upstream analyst hop, `ai/orchestrator/tasks/example-parallel.json` now demonstrates parallel analysts without an automatic reviewer stage, and `scripts/claude-orchestrator.py` now treats a single reviewer-only plan as intentional instead of emitting the read-only reviewer warning. Updated the orchestrator guide/spec plus hot-state memory, and revalidated with Python compile/tests, tracked sample validate plus dry-run runs, doc consistency, and AI memory refresh/check.
- `2026-04-08T20:50:00+02:00`: Completed `WP16` for the local orchestrator spike with retained live run `20260408T184403Z-wp16-live-run-policy-proof-4f22bffe`. Added tracked plan `ai/orchestrator/tasks/wp16-live-run-policy-proof.json`, validated and dry-ran it, then ran it live to prove between-batch `runPolicy` enforcement on a tiny read-only workflow. The first analyst task completed on `claude-haiku-4-5`, cost `$0.079901`, wrote a `921 B` worker-result file and `2642 B` total artifacts, triggered the configured artifact warning plus budget stop, and blocked the second task before the next batch. Inventory confirmed retained-run `governanceStatus = stop` and the new topology summary fields. Updated the spike, orchestrator guide, hot-state memory, and validation ledger, then revalidated with Python compile/tests, tracked plan validate plus dry-run run, live run, inventory, doc consistency, and AI memory refresh/check.
- `2026-04-09T14:35:00+02:00`: Started the CSV spike in code with a bounded Phase 1 adapter. `PojoLensCsv` now loads UTF-8 CSV into typed rows with strict header-based coercion, row/column-aware errors, nested-path projection reuse, and aligned public docs/tests that keep CSV framed as a boundary adapter into the existing engine. Added tracked plan `ai/orchestrator/tasks/wp17-csv-typed-loader-slice.json` as the first practical write-capable `runPolicy` slice; validate and dry-run resolve it to a lean implementer-plus-reviewer DAG with `runBudgetUsd = 2.0` and `1482` estimated prompt tokens, but no live `WP17` run has been executed yet.
